<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.senai.sp.jandira.imcapp20">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@drawable/bmi"
        android:label="@string/app_name"
        android:roundIcon="@drawable/bmi"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity_novo_usuario" />
        <activity android:name=".dash_board" />
        <activity android:name=".ui.ResultadoNcdActivity" />
        <activity android:name=".ui.NcdActivity" />
        <activity android:name=".ui.ResulatdoImcActivity" />
        <activity android:name=".ui.ImcActivity" />
        <activity android:name=".ui.MainActivity" />
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>